import { StyleSheet } from 'react-native';

export const TextStyle = StyleSheet.create({
    title: {
        fontSize: 32,
        fontWeight: 'bold',
        textAlign: 'center',
    },
    errorMessage: {
        color: 'red',
        textAlign: 'center',
        marginTop: 8,
    }
})