import { StyleSheet } from 'react-native';

export const InputStyle = StyleSheet.create({
    input: {
        backgroundColor: '#F7F7FC',
        height: 48,
        borderColor: '#ccc',
        borderWidth: 1,
        paddingHorizontal: 8,
        borderRadius: 6,
    }
})